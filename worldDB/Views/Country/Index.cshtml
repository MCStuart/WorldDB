@using WorldDB.Models;

@if (Model.Count == 0)
{
    <h1>There are no countries now that everyone has joined the hivemind.</h1>
}

@if (Model.Count != 0)
{
  @foreach(var country in Model)
    {
        <li>@country.Name</li>
    }
}
